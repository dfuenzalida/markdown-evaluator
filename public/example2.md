# Example 2

This is another TypeScript example:

```
let result = 2 + 2;
let templateString: string = `This string references the variable 'result' with the value: ${result}`;
console.log(templateString);

```

Another example paragraph here.

---

[Back to the demo](/index.html?file=demo.md)